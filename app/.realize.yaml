settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: go_chat_server
  path: .
  commands: 
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
